importScripts('route-state.js','route-queue.js','signal-protocol.js','repeat-policy.js');
const ROUTE_QUEUE_KEY='parrotRouteQueue',TARGETS_KEY='parrotTargets',EVENTS_KEY='parrotEvents',ROUTE_ALARM='parrot-route-queue',ROUTE_PERIOD_MINUTES=1,MAX_ROUTE_RECORDS=300,MAX_EVENTS=500;
const loadQueue=async()=>{const d=await chrome.storage.local.get(ROUTE_QUEUE_KEY);return Array.isArray(d[ROUTE_QUEUE_KEY])?d[ROUTE_QUEUE_KEY]:[];},saveQueue=async q=>chrome.storage.local.set({[ROUTE_QUEUE_KEY]:q});
async function loadTargets(){const d=await chrome.storage.local.get(TARGETS_KEY);return Array.isArray(d[TARGETS_KEY])?d[TARGETS_KEY]:[];}async function findTarget(id){return(await loadTargets()).find(t=>t.id===id||t.routeKey===id||t.workerLabel===id)||null;}async function patchTarget(id,patch){const ts=await loadTargets(),i=ts.findIndex(t=>t.id===id||t.routeKey===id||t.workerLabel===id);if(i<0)return null;ts[i]={...ts[i],...patch};await chrome.storage.local.set({[TARGETS_KEY]:ts});return ts[i];}
async function appendEvent(e){const d=await chrome.storage.local.get(EVENTS_KEY),a=Array.isArray(d[EVENTS_KEY])?d[EVENTS_KEY]:[];await chrome.storage.local.set({[EVENTS_KEY]:[...a,e].slice(-MAX_EVENTS)});}
async function handleSignal(href,sourceUrl){const signal=ParrotSignalProtocol.parse(href);if(!signal)return{ok:false,reason:'invalid_signal'};const queue=await loadQueue(),ed=await chrome.storage.local.get(EVENTS_KEY),events=Array.isArray(ed[EVENTS_KEY])?ed[EVENTS_KEY]:[];if(queue.some(i=>i.signalId===signal.signalId)||events.some(e=>e.signalId===signal.signalId))return{ok:true,deduped:true};if(signal.kind==='complete'){const targets=await loadTargets(),index=targets.findIndex(t=>t.runId===signal.runId);if(index>=0){targets[index]={...targets[index],status:'completed',completedAt:Date.now(),completionSeenSignal:href,completionSignalType:'link'};await chrome.storage.local.set({[TARGETS_KEY]:targets});}await appendEvent({type:'COMPLETE',signalId:signal.signalId,runId:signal.runId,at:Date.now(),sourceUrl});return{ok:true,matchedTarget:index>=0};}const target=await findTarget(signal.to);if(!target)return{ok:false,reason:'target_not_found'};const payload=signal.kind==='wake'?String(target.wakePrompt||'계속 작업하세요.'):ParrotSignalProtocol.applyTemplate(target.messageTemplate||'[앵무새 | {{FROM}} → {{TO}}]\n\nSOURCE: {{SOURCE_URL}}\n\n{{MESSAGE}}',{FROM:signal.sourceRunId,TO:signal.to,SOURCE_URL:sourceUrl||'',MESSAGE:signal.ref?`REFERENCE: ${signal.ref}`:''});const item={queueId:crypto.randomUUID(),signalId:signal.signalId,type:signal.kind.toUpperCase(),from:signal.sourceRunId,targetRouteId:signal.to,sourceUrl:sourceUrl||'',reference:signal.ref||'',payload,status:'pending',createdAt:Date.now()};await saveQueue(ParrotRouteState.pruneRouteRecords([...queue,item],MAX_ROUTE_RECORDS));await appendEvent({type:item.type,signalId:signal.signalId,queueId:item.queueId,from:item.from,to:item.targetRouteId,at:item.createdAt});queueApi.processEligible().catch(()=>{});return{ok:true,queueId:item.queueId};}
async function applyRouteCooldown(target,cd){if(target.cooldownEnabled===false)return null;const same=target.cooldownKind===cd.kind,attempt=same?Number(target.cooldownStep??target.cooldownAttempt??0):0,next=ParrotRepeatPolicy.nextCooldown(cd.kind,attempt);return patchTarget(target.id,{cooldownKind:next.kind,cooldownStep:next.attempt,cooldownUntil:next.until,lastError:cd.code||cd.kind});}
async function dispatchRouteItem(item){if(!ParrotRouteState.canAutoDispatch(item))return{ok:false,skipped:true,reason:'not_pending'};const target=await findTarget(item.targetRouteId||item.to);if(!target?.url)return{ok:false,reason:'target_not_found'};if(target.cooldownEnabled!==false&&!ParrotRepeatPolicy.cooldownDue(target))return{ok:false,skipped:true,reason:'cooldown_active',cooldownUntil:target.cooldownUntil};let origin;try{origin=new URL(target.url).origin;}catch{return{ok:false,reason:'invalid_target_url'};}const tabs=await chrome.tabs.query({url:`${origin}/*`}),tab=tabs.find(c=>c.url===target.url)||tabs[0];if(!tab?.id)return{ok:false,reason:'target_tab_not_open'};const structure=ParrotRouteState.classifyTabStructure(tab);if(structure.structuralState!=='open')return{ok:false,reason:structure.structuralState,structure};try{const response=await chrome.tabs.sendMessage(tab.id,{type:'PARROT_ROUTE_DELIVER',route:item});if(response?.ok){if(target.cooldownKind||target.cooldownUntil)await patchTarget(target.id,{cooldownKind:'',cooldownStep:0,cooldownUntil:null,lastError:''});return{ok:true,response};}if(response?.reason==='cooldown'&&response.cooldown)await applyRouteCooldown(target,response.cooldown);return{ok:false,reason:response?.reason||'content_rejected',response};}catch(error){return{ok:false,reason:'content_unreachable',error:String(error?.message||error)};}}
const queueApi=ParrotRouteQueue.createRouteQueueApi({load:loadQueue,save:saveQueue,dispatch:dispatchRouteItem,maxRecords:MAX_ROUTE_RECORDS});async function ensureAlarm(){const e=await chrome.alarms.get(ROUTE_ALARM);if(!e)await chrome.alarms.create(ROUTE_ALARM,{periodInMinutes:ROUTE_PERIOD_MINUTES});}chrome.runtime.onInstalled.addListener(()=>ensureAlarm());chrome.runtime.onStartup.addListener(()=>ensureAlarm());ensureAlarm();chrome.alarms.onAlarm.addListener(a=>{if(a.name===ROUTE_ALARM)queueApi.processEligible().catch(()=>{});});
chrome.runtime.onMessage.addListener((message,sender,sendResponse)=>{const run=async()=>{switch(message?.type){case'PARROT_SIGNAL_DISCOVERED':return handleSignal(message.href,message.sourceUrl||sender?.tab?.url||'');case'PARROT_ROUTE_AMBIGUOUS':{const r=await queueApi.reconcileAmbiguity(message.receipt);return{...r,ack:r.ok===true,queueId:message.receipt?.queueId||''};}case'PARROT_ROUTE_DELIVERED':{const item=await queueApi.recordDelivered(message.queueId,message.receipt||{});return{ok:Boolean(item),ack:Boolean(item),item};}case'PARROT_ROUTE_RETRY':return queueApi.manualRetry(message.queueId);case'PARROT_ROUTE_RESOLVE':return queueApi.manualResolve(message.queueId);case'PARROT_ROUTE_PROCESS':return{ok:true,results:await queueApi.processEligible()};case'PARROT_TAB_STRUCTURE':return{ok:true,structure:ParrotRouteState.classifyTabStructure(sender?.tab)};default:return null;}};if(message?.type!=='PARROT_SIGNAL_DISCOVERED'&&!message?.type?.startsWith?.('PARROT_ROUTE_')&&message?.type!=='PARROT_TAB_STRUCTURE')return false;run().then(r=>sendResponse(r)).catch(error=>sendResponse({ok:false,reason:'background_error',error:String(error?.message||error)}));return true;});
